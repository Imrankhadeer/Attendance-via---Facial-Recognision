<UserControl x:Class="FaceAttendance.UI.Views.RegisterView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Camera Feed -->
        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,0,10,0">
            <StackPanel VerticalAlignment="Center">
                 <!-- Image control for camera feed -->
                 <Image Source="{Binding CameraFeed}" Stretch="Uniform" Height="400"/>
                 <TextBlock Text="Camera Feed" HorizontalAlignment="Center" Margin="5"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Register New Student" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <TextBlock Text="Full Name"/>
            <TextBox Text="{Binding StudentName, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Padding="5"/>

            <TextBlock Text="Student ID"/>
            <TextBox Text="{Binding StudentId, UpdateSourceTrigger=PropertyChanged}" Margin="0,5,0,10" Padding="5"/>

            <Button Content="Capture &amp; Register" Command="{Binding CaptureAndRegisterCommand}" Padding="10" Margin="0,20,0,10" Background="#3498DB" Foreground="White" FontWeight="Bold"/>

            <TextBlock Text="{Binding StatusMessage}" Foreground="Red" TextWrapping="Wrap" FontSize="14"/>
        </StackPanel>
    </Grid>
</UserControl>
