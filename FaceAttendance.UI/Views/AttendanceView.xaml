<UserControl x:Class="FaceAttendance.UI.Views.AttendanceView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>

        <!-- Camera Feed -->
        <Border BorderBrush="Gray" BorderThickness="1" Margin="0,0,10,0">
            <StackPanel VerticalAlignment="Center">
                 <Image Source="{Binding CameraFeed}" Stretch="Uniform" Height="400"/>
                 <TextBlock Text="Scanning..." HorizontalAlignment="Center" Margin="5"/>
            </StackPanel>
        </Border>

        <!-- Recognition Result -->
        <StackPanel Grid.Column="1" Margin="10">
            <TextBlock Text="Attendance Mode" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>

            <Border Background="#F0F3F4" Padding="15" CornerRadius="5">
                <StackPanel>
                    <TextBlock Text="Identified:" Foreground="Gray"/>
                    <TextBlock Text="{Binding LastRecognizedName}" FontSize="24" FontWeight="Bold" Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Confidence:" Foreground="Gray"/>
                    <TextBlock Text="{Binding ConfidenceScore}" FontSize="16" Margin="0,0,0,10"/>

                    <TextBlock Text="Status:" Foreground="Gray"/>
                    <TextBlock Text="{Binding ResultMessage}" FontSize="16" Foreground="#2ECC71" TextWrapping="Wrap"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
