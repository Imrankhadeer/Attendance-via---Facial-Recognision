<UserControl x:Class="FaceAttendance.UI.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Dashboard" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1">
            <!-- Stats Cards -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                <Border Background="#3498DB" Padding="20" CornerRadius="5" Margin="0,0,10,0" Width="200">
                    <StackPanel>
                        <TextBlock Text="Total Students" Foreground="White"/>
                        <!-- Bind to a property in VM if available, currently I didn't verify TotalStudents property exposure -->
                        <TextBlock Text="{Binding TotalStudents}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
                <Border Background="#2ECC71" Padding="20" CornerRadius="5" Margin="0,0,10,0" Width="200">
                    <StackPanel>
                        <TextBlock Text="Present Today" Foreground="White"/>
                        <TextBlock Text="{Binding PresentToday}" Foreground="White" FontSize="24" FontWeight="Bold"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <TextBlock Text="Recent Activity" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
            <DataGrid ItemsSource="{Binding RecentActivity}" AutoGenerateColumns="False" CanUserAddRows="False" IsReadOnly="True">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Name" Binding="{Binding StudentName}" Width="*"/>
                    <DataGridTextColumn Header="ID" Binding="{Binding StudentId}" Width="100"/>
                    <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat={}{0:yyyy-MM-dd}}" Width="100"/>
                    <DataGridTextColumn Header="Time" Binding="{Binding Time, StringFormat=hh\\:mm}" Width="100"/>
                    <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
